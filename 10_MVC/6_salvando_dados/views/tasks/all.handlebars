<h1>Todas as tasks</h1>

<div class="container">
    <ul>
        {{#each tasks}}
        <li>
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
        </li>
        {{/each}}
    </ul>
</div>